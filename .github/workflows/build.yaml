name: Build GKE

on:
  push:
    tags:
      - "*"
      # - "^v*"
      # - "^vd+.d+.d+-rc.d+$"

jobs:
  post-release:
    name: "Post release"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Build
        run: |
          echo "tag: " ${GITHUB_REF_NAME}